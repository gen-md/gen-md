---
output: README.md
context:
  - ./package.json
  - ./gitgen.ts
  - ./prompts/branch-plan.md
  - ./prompts/branch-file.md
---
Generate a README.md for git gen.

Structure:
1. Title: "# git gen"
2. Tagline: "Generative git."
3. Install command
4. Brief note: uses git's extension mechanism (git-gen in PATH → git gen)
5. "Why git?" section: git history = implicit style guide, file tree = architecture, related files = local patterns. Generation reads git log, ls-files, sibling files. Workflow: branch → generate → review → commit → push.
6. E2E example showing: create branch + generate, add more, git commit/push
7. Commands table
8. Options list
9. Spec Format (minimal YAML example)
10. MIT license

Keep it minimal and scannable. Use `git gen` for all commands.
