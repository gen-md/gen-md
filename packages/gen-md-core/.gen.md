---
name: gen-md-core-package
description: Generate README.md for @gen-md/core - TypeScript core library
context:
  - "./package.json"
  - "./src/index.ts"
  - "./src/types/index.ts"
  - "./src/parser/index.ts"
  - "./src/resolver/index.ts"
  - "./src/compactor/index.ts"
  - "./src/validator/index.ts"
  - "./src/prompt/index.ts"
  - "./src/git/index.ts"
  - "./src/github/index.ts"
  - "./src/pr/index.ts"
  - "./src/utils/merge.ts"
output: README.md
---
Generate a README.md for @gen-md/core.

## Package
- Name: @gen-md/core
- Version: 0.1.0
- License: MIT
- Dependency: js-yaml

## Exports

### Classes & Factories
| Class | Factory | Description |
|-------|---------|-------------|
| `GenMdParser` | `parser` (instance) | Parse .gen.md files |
| `CascadingResolver` | `createResolver(opts?)` | Resolve cascade chains |
| `Compactor` | `createCompactor(opts?)` | Merge multiple files |
| `GenMdSerializer` | `createSerializer()` | Serialize to .gen.md |
| `Validator` | `createValidator(opts?)` | Validate files |
| `PromptGenerator` | `createPromptGenerator(opts?)` | Generate conversational prompts |
| `GitContextExtractor` | `createGitExtractor(opts?)` | Extract git context (branch, commits, files) |
| `GitHubClient` | `createGitHubClient(auth)` | GitHub API client for PR integration |
| `PRGenerator` | `createPRGenerator(opts?)` | Generate PR-ready output |

### Types
| Type | Fields |
|------|--------|
| `OneShotExample` | input, output |
| `GenMdFile` | filePath, frontmatter, body, examples, raw |
| `GenMdFrontmatter` | name?, description?, context?, skills?, prompt?, output? |
| `ResolvedGenMdConfig` | chain, frontmatter, body, resolvedContext, resolvedSkills |
| `ArrayMergeStrategy` | "concatenate" \| "prepend" \| "replace" \| "dedupe" \| "dedupe-last" |
| `BodyMergeStrategy` | "append" \| "prepend" \| "replace" |
| `PromptOptions` | examplesMerge?, includeCurrentOutput?, userPrompt? |
| `GeneratedPrompt` | prompt, examples, body, currentCode, sourcePath |
| `ValidationResult` | genMdPath, outputPath, passed, errors[], warnings[] |
| `GitCommit` | hash, subject, body, author, authorEmail, date, files |
| `GitContext` | commits, branch, repoRoot, remoteUrl |
| `GitHubAuth` | token, source |
| `GitHubPR` | number, title, body, state, base, head, author, mergedAt, files, url |
| `PRExample` | extends OneShotExample with prNumber, prTitle, prUrl, mergedAt |
| `GeneratedPR` | title, body, base, head, files, labels, sourcePath |
| `EnhancedPromptOptions` | extends PromptOptions with includeGit?, gitOptions?, includePRExamples? |
| `EnhancedGeneratedPrompt` | extends GeneratedPrompt with gitContext, prExamples |

### Utilities
- `mergeArrays<T>(parent, child, strategy)`
- `mergeBody(parent, child, strategy)`
- `deduplicateArray<T>(arr)`
- `formatValidationResults(results)`
- `formatGitContextForPrompt(context)`
- `formatPRForGhCli(pr)`
- `resolveGitHubAuth()` - Resolve auth from GITHUB_TOKEN, GH_TOKEN, or gh CLI
- `parseGitHubRepo(remoteUrl)` - Parse owner/repo from git remote URL

## README Sections
1. Installation: `npm install @gen-md/core`
2. Quick Start example
3. API reference with code examples
4. Types table
5. Git/GitHub Integration section
6. Link to monorepo
