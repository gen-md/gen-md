---
name: gen-md-antigravity-package
description: Configuration for generating gen-md Google IDX (Antigravity) extension
---
<input>
Generate a Google IDX extension for gen-md.

## Package Type: Google IDX Extension

This is a VS Code-compatible extension for Google IDX (Project Antigravity).

### Structure Requirements
```
gen-md-antigravity/
├── src/
│   ├── extension.ts       # Extension entry point
│   ├── commands/          # Command implementations
│   └── providers/         # VS Code-compatible providers
├── syntaxes/
│   └── gen-md.tmLanguage.json
├── language-configuration.json
├── package.json           # VS Code extension manifest
├── tsconfig.json
└── README.md
```

### IDX Compatibility Notes
- Google IDX uses VS Code extension API
- Most VS Code extensions work directly in IDX
- Use web-compatible dependencies only
- Avoid Node.js-specific APIs where possible

### Code Conventions
- Same structure as gen-md-vscode
- Import @gen-md/core (if available in IDX)
- Or include browser-compatible parser
- Use vscode.* API for all functionality

### Features to Implement
- Syntax highlighting for .gen.md files
- Generate command in command palette
- Cascade chain visualization
- Validation on save

### IDX-Specific Considerations
- Test in IDX environment
- Ensure web worker compatibility
- Handle cloud workspace paths
- Support IDX workspace settings
</input>
